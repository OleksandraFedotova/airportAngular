export class Stewardess {
    id: string;
    firstName: string;
    lastName: string;
    birthDate: string;

    constructor(id: string, firstName: string, lastName: string, birthDate: string) {
        this.id = id,
            this.firstName = firstName,
            this.lastName = lastName,
            this.birthDate = birthDate
    }
}